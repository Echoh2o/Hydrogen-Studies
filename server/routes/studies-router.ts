import { studiesController } from "../controllers/studies-controller";

// Export the router from the controller
// This maintains compatibility with existing imports while using the new controller structure
export default studiesController.router;
