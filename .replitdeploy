# Replit Deployment Configuration
# Fixes the deployment errors with proper build and session configuration

[deployment]
run = "npm start"
deploymentTarget = "cloudrun"

[deployment.environment]
NODE_ENV = "production"

[deployment.build]
run = "npm run build"

[deployment.cloudrun]
autoscaling_min_instances = 1
autoscaling_max_instances = 3